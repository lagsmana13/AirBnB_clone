#!/usr/bin/python3
"""This module initializes the package."""
from models.engine.file_storage import FileStorage

# Create an instance of FileStorage
storage = FileStorage()

# Reload the stored data from the file
storage.reload()
